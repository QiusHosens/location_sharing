apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: location-sharing
spec:
  type: NodePort
  selector:
    app: admin
  ports:
    - port: 80
      targetPort: 80
      nodePort: 40081
