# 复制为 secrets.yaml 并修改敏感字段后应用: kubectl apply -f secrets.yaml
# 切勿将 secrets.yaml 提交到版本库
apiVersion: v1
kind: Secret
metadata:
  name: location-secrets
  namespace: location-sharing
type: Opaque
stringData:
  # 需与下方 postgres-password 一致
  database-url: "postgres://postgres:changeme-postgres@postgres:5432/location_sharing"
  redis-url: "redis://redis:6379"
  postgres-password: "changeme-postgres"
  jwt-secret: "changeme-jwt-user"
  jwt-admin-secret: "changeme-jwt-admin"
